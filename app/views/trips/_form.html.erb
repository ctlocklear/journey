<%= simple_form_for(@trip, html: {class:"form-control"}) do |f| %>
  <% if @trip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trip.errors.count, "error") %> prohibited this trip from being saved:</h2>

      <ul>
      <% @trip.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.input :user_id %>
  <%= f.input :trip_name %>
  <%= f.input :destination %>

  
  
  <div class="actions">
    <%= f.button :submit %>
  </div>
</div>
<% end %>
