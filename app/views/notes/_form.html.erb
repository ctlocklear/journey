<%= simple_form_for(@note, html: {class:"form-control"}) do |f| %>
  <% if @note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@note.errors.count, "error") %> prohibited this note from being saved:</h2>

      <ul>
      <% @note.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.input :content %>


  <%= f.input :trip_id, collection: Trip.all, label_method: :trip_name %>


  
  
  <div class="actions">
    <%= f.button :submit %>
  </div>
</div>
<% end %>
